// Essa página é exibida quando ocorrer carregamento de algum recurso

export default function Loading() {
    return (
        <main className="flex items-center justify-center">
            <h1 className="text-2xl">Carregando...</h1>
        </main>
    )
}
